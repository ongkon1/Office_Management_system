import {describe,expect,it} from 'vitest';import {allocationWarning,assignmentEffective,taskDerivedFacts,validateAssignment,validateProject,validateTaskForTime,validateTaskScope} from './rules';
describe('Phase 3 organization and work invariants',()=>{it('requires valid temporary assignment dates and warns without rewriting allocation',()=>{expect(validateAssignment({startDate:'2026-09-02',endDate:null,isTemporary:true,allocationPercent:50})?.status).toBe('validation_failure');expect(allocationWarning(80)[0].code).toBe('ALLOCATION_NOT_100_PERCENT');expect(assignmentEffective({startDate:'2026-09-01',endDate:'2026-09-30',isActive:true},'2026-10-01')).toBe(false)});it('enforces one project division and effective manager scope',()=>expect(validateProject({divisionId:'d1',managerDivisionIds:['d2'],completionPercent:0,startDate:null,endDate:null})?.fieldErrors[0].code).toBe('MANAGER_OUT_OF_SCOPE'));it('enforces task/project/assignee scope',()=>expect(validateTaskScope({taskDivisionId:'d1',projectDivisionId:'d2',assigneeDivisionIds:['d2']})?.fieldErrors[0].code).toBe('TASK_PROJECT_DIVISION_MISMATCH'));it.each(['pending_review','rejected'] as const)('refuses time for %s tasks',reviewState=>expect(validateTaskForTime({projectActive:true,projectAcceptsTime:true,authorized:true,reviewState})?.status).toBe('validation_failure'));it('derives actual minutes and overdue state',()=>expect(taskDerivedFacts([{activeMinutes:30,isActive:true},{activeMinutes:40,isActive:false}],'pending','2026-09-01','2026-09-02')).toEqual({actualMinutes:30,isOverdue:true}));});
